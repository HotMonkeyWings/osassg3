In order to compile, 

run gcc Bankers.c && ./a.out

Then Input the Values for process resources and follow along.

This calculated by the general process that can be seen in the 3 separate functions used.

a) It calculates one of the SAFE STATE sequences and outputs it.

b) It checks whether the given process sequence is valid

c) It checks whether an inputted process can be run in SAFE STATE after the earlier sequence has been accomplished.


